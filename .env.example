# ============================================================================
# USAGE
# ----------------------------------------------------------------------------
# 1) Copy this file to ".env" in the project root.
# 2) Fill in real values (especially secrets) before running.
# 3) Do NOT commit the ".env" file.
# Note: If your config loader expects dotenv format, convert
#       "KEY: value" into "KEY=value" when creating .env.
# ============================================================================

# ============================================================================
# APP CONFIG
# ============================================================================
# App environment: development, production, test
NODE_ENV: development
# Server port
PORT: 3000
# Application name (for logging, emails, etc.)
APP_NAME: My Awesome API
# Application base URL (for generating links in emails, etc.)
APP_URL: http://localhost:3000

# CORS: comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS: http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# SECURITY & AUTH
# ============================================================================
# JWT secret used to sign tokens. Use a strong random string.
# Tip to generate: openssl rand -base64 32
JWT_SECRET: change_me_to_a_strong_random_value
# JWT expiry (e.g., 15m, 1h, 1d)
JWT_EXPIRES_IN: 1d

# Optional: basic rate limiting to protect against abuse
RATE_LIMIT_WINDOW_MS: 60000
RATE_LIMIT_MAX: 100

# ============================================================================
# DATABASE
# ============================================================================
# Choose one of the following configuration styles.
# STYLE 1: Single connection string
DATABASE_URL: postgresql://user:change_me@localhost:5432/mydb?schema=public

# STYLE 2: Split configuration (optional)
DB_DIALECT: postgres
DB_HOST: 127.0.0.1
DB_PORT: 5432
DB_USER: admin
DB_PASS: change_me
DB_NAME: my_database

# ============================================================================
# LOGGING
# ============================================================================
# Log level: error, warn, info, http, debug
LOG_LEVEL: debug

# ============================================================================
# REDIS / CACHING (Optional)
# ============================================================================
REDIS_URL: redis://localhost:6379

# ============================================================================
# THIRD-PARTY SERVICES
# ============================================================================
# Email (SMTP)
SMTP_HOST: smtp.mailtrap.io
SMTP_PORT: 2525
# Leave empty if not used
SMTP_USER:
SMTP_PASS:
SMTP_FROM_NAME: My Awesome API
SMTP_FROM_EMAIL: noreply@example.com

# AWS S3 (Optional)
AWS_ACCESS_KEY_ID:
AWS_SECRET_ACCESS_KEY:
AWS_REGION: ap-southeast-1
S3_BUCKET_NAME:
